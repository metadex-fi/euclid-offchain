"use strict";
// import { fromHex, toHex } from "https://deno.land/x/lucid@0.8.6/mod.ts";
// export function toText_(hex: string): string {
//   return new TextDecoder().decode(fromHex(hex));
// }
// export function fromText_(text: string): string {
//   return toHex(new TextEncoder().encode(text));
// }
